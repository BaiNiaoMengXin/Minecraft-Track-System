{
	"format_version": "1.8.0",
	"animations": {
		"animation.mts.rotation": {
			"loop": true,
			"bones": {
				"root": {
					"rotation": ["math.mod(math.mod(math.lerp(v.prev_x_rot, v.x_rot, query.frame_alpha), 360) + 360, 360)", "math.mod(math.mod(math.lerp(v.prev_y_rot, v.y_rot, query.frame_alpha), 360) + 360, 360)", "math.mod(math.mod(math.lerp(v.prev_z_rot, v.z_rot, query.frame_alpha), 360) + 360, 360)"]
				}
			}
		}
	}
}